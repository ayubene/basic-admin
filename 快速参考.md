# 快速参考

## 🚀 启动项目
```bash
npm run dev
```
访问: http://localhost:5175/

## 🔑 Token 管理

### 设置 Token
```typescript
import { setToken } from '@/utils/auth'
setToken('your-token-here')
```

### 获取 Token
```typescript
import { getToken } from '@/utils/auth'
const token = getToken()
```

### 清除 Token
```typescript
import { clearToken } from '@/utils/auth'
clearToken()
```

### 浏览器控制台测试
```javascript
// 设置
localStorage.setItem('token', 'test-token-123')

// 获取
localStorage.getItem('token')

// 清除
localStorage.removeItem('token')
```

## 📡 接口配置

### 基础 URL
- 开发环境: 通过 Vite 代理
- 生产环境: `http://10.1.23.80:8082/shiye-retail`

### 已配置的接口
| 接口 | 路径 | 说明 |
|------|------|------|
| 用户列表 | `/system/user/list` | 分页查询用户 |
| 部门列表 | `/system/department/departList` | 获取所有部门 |

### 添加新接口
1. 在 `src/api/` 创建 API 文件
2. 在 `vite.config.ts` 添加代理（如果路径不同）
3. 在页面中使用

## 🎨 组件使用

### BasicTable
```vue
<BasicTable
  ref="tableRef"
  :columns="columns"
  query-url="/system/user/list"
  :height="520"
/>
```

### BasicSelect
```vue
<BasicSelect
  v-model="form.field"
  list-url="/system/department/departList"
  value-key="id"
  label-key="name"
  placeholder="请选择"
/>
```

### BasicButton
```vue
<BasicButton 
  type="primary" 
  :on-click="handleClick"
>
  按钮文字
</BasicButton>
```

### BasicModal
```vue
<BasicModal 
  v-model="visible" 
  title="标题"
  width="600px"
>
  内容
</BasicModal>
```

## 🔧 修改 Token 格式

### 当前格式
```
Authorization: Bearer {token}
```

### 修改位置
1. `src/utils/request.ts` (第 56 行)
2. `src/api/config.ts` (第 24 行)

### 其他格式示例
```typescript
// 格式1
config.headers.token = token

// 格式2
config.headers['X-Token'] = token

// 格式3
config.headers['X-Auth-Token'] = token
```

## 📂 重要文件

| 文件 | 说明 |
|------|------|
| `src/utils/request.ts` | BasicComponents 请求配置 |
| `src/api/config.ts` | 自定义 axios 配置 |
| `src/utils/auth.ts` | Token 管理工具 |
| `vite.config.ts` | Vite 代理配置 |
| `src/main.ts` | 应用入口 |

## 🐛 常见问题

### 请求没有带 token
```javascript
// 检查 token 是否存在
console.log(localStorage.getItem('token'))

// 设置 token
localStorage.setItem('token', 'your-token')
```

### 跨域问题
- 开发环境: 已配置 Vite 代理，无跨域问题
- 生产环境: 需要后端配置 CORS

### 401 错误
- Token 无效或过期
- 会自动清除 token
- 需要重新登录

## 📖 详细文档

- `项目配置总结.md` - 完整配置说明
- `Token配置说明.md` - Token 详细指南
- `API接口配置说明.md` - 接口配置详情
- `测试Token.md` - Token 测试步骤

## 💡 提示

1. 所有请求都会自动添加 token
2. BasicTable 和 BasicSelect 的请求也会带 token
3. 401 错误会自动清除 token
4. 可以在浏览器控制台快速测试 token
